**Do not rerun isolated-one radius-6 strips, packed-popcount currents for
\(D\), or \(\varphi_k\)-injectivity against residue 0.** Cycle T killed
those. Square 2-kernel columns are still distinct through \(k=9\). These
are proposals.

1. **Collision or proof for square 2-kernel columns at all \(k\) — Problem 1**

   The \(\varphi_k\) template is dead (\(k=3\)). Distinct length-\(2^k\)
   prefixes through \(k=9\) remain the only infinite-kernel handle.
   Next experiment: \(N=2^{20}\) for \(k=10\) (need index \(2^{20}-1\)).
   Kill on one collision. Survive only with a proof, not \(k=10\) as a
   table.

2. **Stop unless that proof appears.** Local pairings, Condrey fibers,
   onset tables, hull censuses, and driven-\(\sigma\) scans are not new
   mechanisms. Problem 2 is still \(D(N)=o(N)\) with no local current.
   Problem 3 is still the official TM predicate, not a cell-gate cutoff.
