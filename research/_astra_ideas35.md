**Do not rerun the Fermat-odd identity \(c_{q 2^k}=c_{2^k}\oplus J\)
for \(q=2^a+1\), the \(G\)-parity that leftmost-11 hits on
\([2^k,q 2^k)\) are odd, or \(\varphi^{(q)}\) as \(I_k\).** Cycle AK:
period \(2^m\) forces every such \(\varphi^{(q)}=0\);
\(\varphi^{(q)}=1\oplus S^{(q)}\) for every Fermat-odd \(q\); \(S\)
is not identically 0. These are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). On \(k\le 10\) at least
   one of \(q=3,5,9\) is 1 for every \(k\ge 2\); that is not a proof.
   Survive only with a production on infinitely many of these
   intervals.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\).

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
