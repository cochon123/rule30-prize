You previously recommended STOP on this Rule 30 prize program. The human overrode that: be MORE CREATIVE and propose new attacks anyway. Do NOT implement. Do NOT edit files. Do NOT claim a prize. Do NOT contact anyone.

Workspace: /home/cochonhome/Documents/random-shit/wolfram
Read REPORT.md, research/LOG.md, and any note you cite. Your previous stop note is research/_astra_ideas3.md.

## Forbidden (already dead; do not retread)

- Finite-T onset SAT / larger tables / more strip radius
- Ideal membership F_T in <F_{T+j}> with unbounded residual memory
- 1-site/2-site/3-site H-invariants for q=8 wrap I
- Phase-mask (d,h) cycles that lost to h≥d
- Ancestry DFAs (they reconstruct the triangle)
- Block-energy pairing without a Rule 30 identity
- Mahler DAG (it is the CA, Θ(n²))
- Rigidity A_p ⇒ B_p (equivalent to ¬A_p given Jen/Kopra)
- “Try more SAT”

## What we still have

Rule: x' = L XOR (M OR R), seed δ_0, c_t = x(t,0).
Jen/Kopra: no eventually periodic adjacent pair on a nonzero finite seed.
Proved: infinitely many 0s and 1s in c; 01^q excluded for q=7 and q≥9; F_4=0 on Fibonacci u under hypothetical period-2; eventual-vacuum for eventually-zero u.
Open: period 2 for large T, periods 3–7, q=8, all larger periods, density, linear-time computation.

## Required creativity

Propose 5 attacks that change the *mathematical language*, not the width of a table. Examples of the kind of shift wanted (you need not use these):

- Morphic / automatic / kernel complexity of c, or of a transformed sequence (e.g. run-length, discrepancy, 2-adic generating function)
- A conjugacy or factor map to a system whose periodic points are classified
- An invariant of the spacetime that is neither a column nor a finite strip (e.g. a cohomology class, a Lyapunov exponent of the disagreement cocycle, a conserved polynomial on light-cone coordinates)
- Problem 2 via a signed measure or exact cancellation in GF(2)[[x]] that is not block energy
- Problem 3 via an explicit data structure (BDD of a diagonal, straight-line program, online automaton with o(n) states after preprocessing) OR a reduction from a known hard sequence
- Literature you have not used: Sutner, Kari, Culik, Wolfram/Rowland/Lind, topological entropy of CA, permutive CA factor maps, Mahler equations that are *not* the support recurrence already tried
- A lemma whose hypothesis is strictly weaker than “c is not eventually period p” but strictly stronger than “two columns are not both periodic”

For each attack: target problem, why it is not a retread, first assignment specific enough for a subagent, likely obstruction, and a 1-line kill criterion (when to stop that attack).

Rank them. Name which 3 to run first in parallel.

Do not say stop. The human already rejected stop. If an idea is speculative, mark it speculative and still make the first experiment concrete.
