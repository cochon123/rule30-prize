**Do not rerun packed-bit-4/6 Green contributions to \(I_k\), the
identities \(W(2^a,2^{a+1}-2)=1\) and \(W(2^a,2^{a+1}-3)=1\), or the
period-4 forms of \(e_8\) and \(e_{10}\).** Cycle AM: \(I_k=1\oplus
B_k^{\ge 10}\) for \(k\ge 5\); bit 4 is not a closed form (bit 6
cancels); nested \(\le 9\) is not a closed form (bit 10 cancels).
These are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle AM the
   remaining obstruction for \(I_k\) is the bulk of packed bit
   \(\ge 10\) (and, on the scanned prefix, bit 10 itself cancels the
   nested 1). Survive only with a production on infinitely many
   dyadic annuli, not another finite nested-left family.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
