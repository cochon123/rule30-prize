**Do not rerun unique through pentuple Green exhaustiveness on the
3-fold, 5-fold, or 9-fold Fermat annuli, Fermat \(p=1\) times,
Mersenne centre-right unique bits, the covering
\(\varphi^{(3)},\varphi^{(5)},\varphi^{(9)}\) through \(k=15\), the
\(\chi(T)\) closed form, the net bit-1 parity \(P(q,T)\)
recurrences, the even-spine covering-failure criterion through
\(k=12\), \(\gamma_k\) through \(k=15\), the bit-1 cone on \(B,C\),
or low-bit silence / unique \(p=2U+2\) on \(B,C\).**
Cycles BG–BR: those unique through pentuple bits are not
identically-1 productions, \(\Theta(3\cdot 2^k)\) is not identically
1, net packed-bit-1 parity 1 is not a covering production, blocks
\(B,C\) have zero hits from every \(p\le 2U+1\), and the unique
\(p=2U+2\) AND is not identically 1. Nested left is still not a
formula for \(I_k\).
These are proposals.

1. **Prove \(S_A=1\) and \(S_B=S_C=0\) never occur together**

   \(S_B\) and \(S_C\) are now known to live entirely on packed bits
   \(p\ge 2U+2\). A theorem that \(S_A=1\) and \(S_B=S_C=0\) never
   happen together is the Fermat covering for all \(k\ge 3\), which
   kills every eventual period \(2^m\). Do not treat the unique
   \(p=2U+2\) AND as a 1-production. Do not treat
   \(\Theta(6\cdot 2^k)\equiv 1\) as proved. Do not hunt another
   finite nested-left family. Do not push the Fermat covering table
   to \(k=16\).

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
   Septuple families still grow with \(k\); that is the treadmill.
