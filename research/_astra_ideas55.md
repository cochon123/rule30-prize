**Do not rerun the Fermat covering \(\varphi^{(3)},\varphi^{(5)},\varphi^{(9)}\)
through \(k=15\).** Cycle BE: that covering is still a prefix, not a
theorem. Nested left is still not a formula for \(I_k\). Bit 33
contributes 1 (Cycle BD). These are proposals.

1. **Prove the Fermat covering for all \(k\ge 2\), or some
   \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\) infinitely
   often**

   A theorem that for every \(k\ge 2\) at least one of
   \(\varphi^{(3)},\varphi^{(5)},\varphi^{(9)}\) is 1 would kill every
   eventual period \(2^m\). The prefix through \(k=15\) is not that
   theorem. No single Fermat \(q\le 17\) is identically 1 on that
   range. The live remainder in \(B^{\ge 20}\) is still multiplicity
   \(\ge 10\) together with unique \(2\)-family \(j\ge 6\) and unique
   \(3\)-family \(j\ge 4\). Do not hunt another finite nested-left
   family. Do not treat \(I^{\mathrm{left}}=0\) as proved.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
