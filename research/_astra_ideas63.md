**Do not rerun unique through pentuple Green exhaustiveness on the
3-fold, 5-fold, or 9-fold Fermat annuli, Fermat \(p=1\) times,
Mersenne centre-right unique bits, the covering
\(\varphi^{(3)},\varphi^{(5)},\varphi^{(9)}\) through \(k=15\), or
the \(\chi(T)=G(2T-1,3T-1)\) closed form.**
Cycles BG–BM: those unique through pentuple bits are not
identically-1 productions, and \(\Theta(3\cdot 2^k)\) is not
identically 1. Nested left is still not a formula for \(I_k\).
These are proposals.

1. **Prove the Fermat covering for all \(k\ge 2\), or some
   \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\) infinitely
   often**

   A theorem that for every \(k\ge 2\) at least one of
   \(\varphi^{(3)},\varphi^{(5)},\varphi^{(9)}\) is 1 would kill every
   eventual period \(2^m\). Unique through pentuple Green remainder
   is closed on all three covering annuli. Leftmost 11 hits both
   \(\theta_k\) and \(\Theta(3\cdot 2^k)=\varphi^{(9)}_k\oplus\theta_k\),
   but \(\{\theta,\varphi^{(9)}\}\) still fails at \(k=3\) and is
   unproved for \(k\ge 4\). Do not hunt another finite nested-left
   family. Do not treat \(I^{\mathrm{left}}=0\) as proved. Do not
   treat \(c_{5\cdot 2^k}\land r_{5\cdot 2^k}=0\) as proved. Do not
   treat the 9-fold pentuple XOR as \(\varphi^{(9)}\). Do not treat
   unique XOR triple XOR pentuple as \(\theta_k\). Do not treat
   \(\Theta(3\cdot 2^k)\equiv 1\) as proved.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
   Septuple families on the 3-fold and 5-fold annuli still grow with
   \(k\); that is the treadmill, not a covering closed form.
