**Do not rerun unique through pentuple Green exhaustiveness on the
3-fold, 5-fold, or 9-fold Fermat annuli, Fermat \(p=1\) times,
Mersenne centre-right unique bits, the covering
\(\varphi^{(3)},\varphi^{(5)},\varphi^{(9)}\) through \(k=15\), the
\(\chi(T)\) closed form, the net bit-1 parity \(P(q,T)\)
recurrences, the even-spine covering-failure criterion through
\(k=12\), \(\gamma_k\) through \(k=15\), the bit-1 cone on \(B,C\),
low-bit silence / unique \(p=2U+2\) on \(B,C\), the four
leftmost unique bits \(p=2U+2,\ldots,2U+5\) on \(B,C\), or the
freshman-defect identities for \(M=2^a\).**
Cycles BG–BT: those unique through pentuple bits are not
identically-1 productions, blocks \(B,C\) have zero hits from every
\(p\le 2U+1\), the four leftmost unique ANDs on \(B,C\) are not an
identically-1 XOR, and the centre coboundaries on \(B,C\) equal the
Green AND-parities only after adding the freshman palindrome
defects \(d_B,d_C\), which are not identically 0. Nested left is
still not a formula for \(I_k\).
These are proposals.

1. **Prove \(J_A=0\) and \(J_B=d_B\) and \(J_C=d_C\) never occur together**

   Covering fails at \(k+1\) iff those three hold (Cycle BT). The
   defect \(d_B\) is itself the Green remainder on \(A\) targeting
   packed bits \(2U\) and \(10U\). A theorem that the triple never
   happens is the Fermat covering for all \(k\ge 3\), which kills
   every eventual period \(2^m\). Do not drop \(d_B\) or \(d_C\).
   Do not classify further unique bits on \(B,C\) unless a covering
   production appears. Do not hunt another finite nested-left
   family. Do not push the Fermat covering table to \(k=16\).

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
   Septuple families still grow with \(k\); that is the treadmill.
