**Do not rerun 5-window preimages of `000`/`101`, left-diagonal
eventual periodicity as a formula for \(c_t\), or local formulas
already killed in Cycles Z–AC.** Cycle AC: `00` is `000` or `101`;
every \(e_j\) is eventually periodic; \(c_t=e_t(t)\) is an onset.
These are proposals.

1. **Infinitely many centre `00`s — kills isolated-zero periods**

   Infinitely many `000` or `101` about the origin would exclude every
   eventual \(01^q\), including period 2 and \(q=8\). Survive only with
   a CA production that forces one of the eight 5-window preimages
   infinitely often (not a longer prefix). Periods 3–7 already contain
   `00`, so they would remain.

2. **Stop unless that production appears.** The periodic tails of
   \(e_j\) do not compute \(c\). Problem 2 is still \(D(N)=o(N)\).
   Problem 3 is still the official TM predicate.
