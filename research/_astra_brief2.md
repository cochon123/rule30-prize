Round 1 results. You already proposed attacks 1–6. Three were run. READ the new notes, then propose the next attacks. Do NOT implement. Do NOT edit files. Do NOT claim a prize. Do NOT contact anyone.

## Round 1 outcomes

1. Period-2 inductive certificate (research/period2_certificate.md, period2_certificate.py)
   - F_T is in the ideal generated by later F_{T+j} for every 1≤T≤12, with explicit multipliers (linear except T=12; F_4=0).
   - Linear ansatz FAILS at T=16 and T=20.
   - Sound enumeration still kills T≤32; worst still T=20, R=16, F_37.
   - Shift identity: if F_T=1 and F_{T+1}=F_{T+2}=0 then F_{T+3}=1+F_{T+1}(Su). A long L_0 germ maps to a length-2 bump, NOT a smaller L_0. Does not reduce to the vacuum machine.
   - Residual memory scales with T. No well-founded rank (last-1 index and Hamming weight increase). Attack died as a uniform proof.

2. q=8 σ periodicity (research/period9_sigma.md, period9_sigma.py)
   - q=8 NOT excluded.
   - Radius-6 20-state return graph is a FULL 2-shift on σ — label language alone cannot force periodicity.
   - Finite left support at n=0 forces 18-bit reconstruction pairs into a 512-state quotient I whose only recurrent label is wrap=0. That is periodicity of wrap in the DEPTH direction, not of σ in n.
   - Consistent σ-streams never enter I through depth 16 (rank ≤ 3). Every onset T≤12 dies by R=5.
   - Free-boundary mixing is not a seed orbit. Large-T still open.

3. Mahler dependency (research/mahler_dependency.md, mahler_dependency.py)
   - No O(n) algorithm. DAG is Θ(n²) for 2^m, 2^m-1, and general n.
   - Exact zeta cancellation REPRODUCES the original CA (Z_k(s)=u(s,k)). Shared circuit is the right-edge recurrence.
   - Bit/TM/word-Θ(log n) RAM: quadratic. Failure to compress is not a lower bound.

Attacks 3 (phase-mask propagation for 001/0111), 4 (inverse row transducer ancestry), and 5 (block energy / density) from your previous ranking have NOT been run yet.

## Required output

1. Whether any of 3/4/5 is still worth running, or should be retargeted given round 1.
2. 4–6 NEW or retargeted attacks, ranked, same format as before (target, why, first assignment, obstruction).
3. Which 2–3 to run next in parallel.
4. If you now believe problem 1 cannot be finished by these methods, say so and name a qualitatively different approach (or recommend stopping).

Be specific. No “try more SAT”. No claiming the prize is close if the gap is still uniform-in-T / all-periods.
