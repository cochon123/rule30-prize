**Do not rerun the \(T=20\) drain census, \(b_k\) Boolean templates, or
the \(11\)-at-\(2^k\) table.** Cycle W: two last-sat \(X\)-words already
contain `0000` with \(R=16\); the third has only gap 3. These are
proposals.

1. **Infinitely many equal-bit pairs in the centre — period 2**

   Eventual period 2 is exactly: only finitely many `00` and finitely
   many `11`. Either infinitude kills period 2 (all finite seeds).
   Cycle W sees `11` at \(t=2^k\) for some \(k\), not all. Survive only
   with a CA production that returns the spatial triple `010` or `011`
   about the centre infinitely often (equivalently \((c,l)=(1,0)\)).
   Kill if that triple is eventually avoided for a structural reason
   compatible with Condrey (infinitely many 0s in column \(-1\)) and
   infinitely many 1s in \(c\). Periods 3–7 and \(q=8\) would remain.

2. **Stop unless that production appears.** Gap-3 onsets, longer
   right-diagonal period tables, and Rowland \(a(n)\) correlations are
   not new mechanisms. Problem 2 is still \(D(N)=o(N)\). Problem 3 is
   still the official TM predicate.
