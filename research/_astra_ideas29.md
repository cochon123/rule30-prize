**Do not rerun the right-integrator recurrence, the no-right-white-stripe
induction, finite incidence of \(u(*,k)\) in the centred 5-window, the
Green formula for \(d=\ell\oplus r\), or `00` iff \(a=r\lor e\).**
Cycle AE: nested right cannot produce infinitely many centre `00`s;
the right has no white stripes. These are proposals.

1. **A bulk production of infinitely many centre `00`s**

   Both edge-parallel families of bounded depth are now excluded.
   Infinitely many 1-run endings with \(x(-2)=r\lor x(2)\) would kill
   every eventual isolated-zero period, including period 2 and
   \(q=8\). Survive only with a production in the bulk (both offsets
   growing with \(t\)), not an edge tail, a Green rewrite of \(d\), or
   a longer prefix. Periods 3–7 already contain `00` and would remain.

2. **Stop unless that production appears.** Problem 2 is still
   \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
