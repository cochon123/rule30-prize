**Do not rerun no-even-quads, the five-seed odd-lift orbit,
\(e_{19}\), bit 19 contributing 1, or \(I_k=B^{\ge 20}\).** Cycle AZ:
nested depth 19 is not a closed form. Quad XOR is not \(I_k\). These
are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle AZ,
   \(I_k=B_k^{\ge 20}\) for \(k\ge 6\). Multiplicity 1–4 Green bits
   are classified. The live remainder is multiplicity \(\ge 5\) in
   \(B^{\ge 20}\) together with the scaling unique \(2\)-family
   \(j\ge 5\) and unique \(3\)-family \(j\ge 3\) (fires at \(k=15\)).
   Do not hunt another finite nested-left family. Do not treat
   \(I^{\mathrm{left}}=0\) or bit 33 always firing as proved. Do not
   assume every \(e_j\) is eventually period 4.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
