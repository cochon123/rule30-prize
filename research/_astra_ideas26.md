**Do not rerun \(I_k=\) a single-column annulus parity of \(\ell\), of
\(r\), or of \(c\land r\); vacuum-radius gadgets for centre runs; or
local formulas already killed in Cycles Z–AB.** Cycle AB:
\(I_k=\bigoplus(\ell\oplus r\oplus(c\land r))\) and
\(R_k=\bigoplus(\ell\oplus r)\), with \(d=\ell\oplus r\) not eventually
0. These are proposals.

1. **Infinitely many centred `000`s — kills isolated-zero periods**

   A centre `00` is a spatial triple `000` about the origin.
   Infinitely many would exclude every eventual \(01^q\), including
   period 2 and \(q=8\). Survive only with a CA production (not a
   longer prefix of run lengths). Periods 3–7 have `00` already, so
   they would remain. Unbounded 0-runs would solve Problem 1 outright;
   the observed \(\max\sim\log N\) growth is not a construction.

2. **Stop unless that production appears.** Problem 2 is still
   \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
   \(I_k\) not eventually 0 still needs odd defect-block parity
   infinitely often, which infinitely many \(d=1\) does not give.
