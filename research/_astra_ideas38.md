**Do not rerun the dyadic interval \(W(2^a,D)\), the Mersenne
mod-3 form of \(G(2^a-1,d)\), the Fermat 3-sparse form of
\(G(2^a+1,d)\), the two-interval form of \(W(3\cdot 2^a,D)\), or the
time-\(T\) slice as a formula for \(I_k\).** Cycle AN: \(I_k\) splits
by the \(W(T,\cdot)\) support into packed-index halves;
\(I^{\mathrm{left}}=0\) on \(7\le k\le 12\) is a prefix, not a
theorem. These are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle AN the
   remaining obstruction for \(I_k\) is still a bulk Green remainder
   (empirically the packed bits \(p\ge T+2\) on the scanned prefix).
   Survive only with a production on infinitely many dyadic annuli.
   Do not treat \(I^{\mathrm{left}}=0\) as proved, and do not hunt
   another finite nested-left family.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
