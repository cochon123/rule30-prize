**Do not rerun the Jacobsthal half-window \(G(n,2^a-2^b)\), the
Mersenne-odd Green times \(t=T+s\cdot 2^j\), or the \(7\)-family
triples.** Cycle AT: those families are not identically-1
productions and their total XOR is not \(I_k\). These are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle AT the
   Mersenne-odd part of \(I_k\) is explicit and does not yield a new
   forced 1. The obstruction is still non-Mersenne odd \(q\) and even
   packed bits. Survive only with a production on infinitely many
   dyadic or 3-fold annuli. Do not treat \(I^{\mathrm{left}}=0\) as
   proved, and do not hunt another finite nested-left family.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
