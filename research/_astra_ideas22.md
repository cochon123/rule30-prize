**Do not rerun finite-window coboundaries or bond fluxes of range
\(\le 5\).** Cycle X: vacuum kills every \(2c-1\) and pair current
universally and on the prize orbit (a vacuum-5 run of length 5);
Gaussian elimination kills the \(2c\) families as well, including
flux. Spatial 7-windows about the origin are already a full 2-shift
on \(t<2^{14}\). ideas21.1 is not a reduction. These are proposals.

1. **Infinite 2-kernel with a CA splitting lemma — Problem 1**

   Distinct length-\(2^k\) square prefixes through \(k=9\) remain the
   only infinite-kernel handle. Do not run \(k=10\) as a collision
   hunt. Survive only with a proof that for every \(k\) the \(2^k\)
   residue columns disagree (a predicted index from the local rule,
   not a table). Finite kernel would not prove periodicity; an
   infinite kernel would prove aperiodicity.

2. **Stop unless that proof appears.** Local currents, equal-bit-pair
   reductions, onset tables, hull censuses, and longer spatial or
   temporal de Bruijn prefixes are not new mechanisms. Problem 2 is
   still \(D(N)=o(N)\) with no finite-range current: only an extensive
   \(\varphi=o(N)\) or a long-range pairing of \(11\) against \(00\).
   Problem 3 is still the official TM predicate.
