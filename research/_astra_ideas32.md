**Do not rerun the odd-spine half-step, the period constraint on
\((c_{2^k})\), locality of the one-step remainder \(O^{(1)}\), or
annulus XOR of \(O^{(1)}\) as \(I_k\).** Cycle AH: Cycle Z is the
\(q=1\) case of a Freshman half-step at times \(q\cdot 2^k\);
eventual period \(2^m\) forces \((b_k)\) eventually constant;
\(\Delta^{(q)}\) and \(J^{(q)}\) are not closed forms for
\(q\in\{3,5,7,9\}\). These are proposals.

1. **Non-vanishing of \(I_k\)**

   Infinitely many \(I_k=1\) would kill every eventual period \(2^m\),
   including period 2 and isolated-zero \(q=3\). Periods 3, 5, 6, 7
   and \(q=8\) would remain. Survive only with a production of odd
   Green parity on infinitely many dyadic annuli, not a local or
   finite-window formula already killed in Cycles Z–AH.

2. **A bulk production of infinitely many centre `00`s**

   Still infinitely many 1-run endings with \(a=r\lor e\). Nested
   edges and Green remainders \(O_s\), \(O^{(1)}\) are not a second
   handle. This would kill every isolated-zero eventual period,
   including \(q=8\). Periods 3–7 already contain `00` and would
   remain.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
