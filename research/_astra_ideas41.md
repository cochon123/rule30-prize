**Do not rerun the bit-run form of \(G(m,2^a)\), the period
\(2^{a+1}\) pairing, or the unique double-Green bit
\(p=3\cdot 2^{k-1}+1\) on the 3-fold annulus.** Cycle AQ: that
double bit’s firing XOR is not identically 1; five triple-Green bits
are a prefix. These are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle AQ the
   unique- and double-Green parts of \(\theta_k\) are explicit and
   do not yield a new forced 1. The obstruction is still multiplicity
   \(\ge 3\). Survive only with a production on infinitely many
   dyadic or 3-fold annuli. Do not treat \(I^{\mathrm{left}}=0\) as
   proved, and do not hunt another finite nested-left family.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
