**Do not rerun Fermat-subtract \(G(n,2^a-2^c-1)\), the derived
half-windows \(2^a-6,7,10,12\), the period-4 tails of \(e_{11}\) and
\(e_{12}\), or the contributions of packed bits 10, 11, 12.** Cycle
AU: \(I_k=1\oplus B_k^{\ge 13}\) for \(k\ge 5\), and nested depth 12
is not a closed form. These are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle AU the
   nested-left remainder through packed bit 12 is explicit and still
   has a bulk \(B^{\ge 13}\). Do not hunt another finite nested-left
   family. Survive only with a production on infinitely many dyadic
   or 3-fold annuli. Do not treat \(I^{\mathrm{left}}=0\) as proved.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
