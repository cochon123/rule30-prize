**Do not rerun even pentuples, the eight-seed families, \(e_{20}\)–\(e_{27}\)
period 4, \(e_{30}\equiv 1\) for \(t\ge 33\), or bits 20, 24, 25 never
firing.** Cycle BA: pentuple XOR is not \(I_k\). Nested left is still
not a formula for \(I_k\). These are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle BA,
   multiplicity 1–5 Green bits are classified. The live remainder in
   \(B^{\ge 20}\) is multiplicity \(\ge 6\) together with the scaling
   unique \(2\)-family \(j\ge 5\) (bit 33 firing at \(T\) is still
   PREFIX through \(k=12\)) and unique \(3\)-family \(j\ge 4\) (the
   \(j=3\) member \(p=25\) never fires; the family still fires at
   \(k=15\) on \(j=k-3\)). Do not hunt another finite nested-left
   family. Do not treat \(I^{\mathrm{left}}=0\) or bit 33 always
   firing as proved. Do not assume every \(e_j\) is eventually
   period 4 (\(e_{29}\) has period 8). Even \(|S|=6\) dies after
   \(a=7\); even \(|S|=7\) still grows.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
