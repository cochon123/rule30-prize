**Do not rerun \(v_2\)/popcount kernel-index formulas, and do not treat a
larger de Bruijn prefix as a prize proof.** Cycle U: every word of
length \(\le 14\) occurs; twelve length-15 words are missing at
\(N=2^{18}\). These are proposals.

1. **Both right-extensions of every factor — Problem 1**

   If every factor \(W\) has both \(W0\) and \(W1\) as factors, the
   language is the full 2-shift and \(c\) is disjunctive, hence not
   eventually periodic. Cycle U gives this through \(|W|\le 13\) (since
   \(p(14)=2^{14}\)). Kill if some \(W\) has a unique continuation that
   is forced by Rule 30 (a centre-only predictor; distinct from
   `stream_nextbit.md` packed-row statistics). Survive only with a
   construction that, for every \(W\), produces occurrences of both
   extensions from the single-1 seed.

2. **Stop unless that construction appears.** Square \(k=10\) is
   implied by existing length-128 kernel prefixes. Another onset table
   is not a new mechanism.
