**Do not run another origin-in-hull census.** Cycle O killed bump-rank for `L_0` and showed unique-left infinitude is the same onset problem. The remaining period-2 handle is a uniform `R(T)` or a seed-specific constraint that arbitrary Fibonacci `u` omits. These are proposals; the certifiers are the authority.

1. **Morse–Hedlund rigidity of long `F`-zero runs — Problem 1**

   Cycle O: an `L_0` with `R≥4` forces `F(Su)` to be `011` then zeros at the same columns, with no rank. Cycle D: last-sat words at `T=20` share a period-3 tail; mixed masks at other `T` proliferate. Ask whether `R≥R_0` forces the factor complexity of the `u`-prefix of length `nvars(T+R)` to satisfy `p(n)≤n` (eventually periodic). An infinite zero tail would then force eventually periodic `u`, already excluded by Jen plus eventual-vacuum.

   **First experiment:** on every last-sat model of `T≤34`, compute `p(n)` of the forced `u`-mask and of the free bits. Kill if some model with `R≥12` has `p(n)>n` for all `n≤|u|/2` (positive-entropy germ). Survive only with a lemma that `R≥R_0` implies a periodic tail of bounded period, covering mixed masks such as `T=16`.

2. **Realizable `u` from a finite right, not the full Fibonacci shift — Problem 1**

   `L_0` is currently quantified over every Fibonacci `u`. A period-2 centre on a finite seed only needs those `u` produced by a finite-support right half under the two-step map. Vacuum `u` is one of them and is not eventually periodic. Seek an extra identity on realizable `u` (for example a bound on gaps of `u`, or a forbidden factor beyond consecutive 1s) that the full Fibonacci shift omits, then rerun the onset scan on that subshift.

   **First experiment:** for every right of width `≤8`, both phases, `T=512`, list the language of length-8 factors of `u` and of even-time triples `(u,e,f)`. Kill if the length-8 language is the full Fibonacci 8-language (every legal word appears). A proper subshift earns a restricted onset scan.

3. **Even subsequence `c_{2n}` not eventually constant — Problem 1**

   Both period-2 phases make `(c_{2n})` eventually constant. Proving that the prize seed’s even times take both values infinitely often would exclude period 2 (and some period-4 words such as `1110`). This is Condrey’s constant-trace theorem for `F^2` on the even lattice, not a restatement of Jen on width 2 of `F`.

   **First experiment:** write the radius-2 map `G=F^2` and test left-permutivity width. If `G` is rapidly left-expansive of width 1, Kopra applies and even-time traces are not eventually periodic. Kill if the inverse of `G` needs a spatial window of width `≥2` (expected: `G(x)_i=x_{i-2}⊕g(x_{i-1..i+2})`, so width `>1`). A width-1 inverse would be the prize.

4. **Dyadic centre bits as a morphic constraint — leftover; Problem 1**

   Empirically `c_{2^n}` is not constant through `n=11`. Seek a closed form or a 2-kernel disagreement at residues `0` and `2^{k-1}` that can be proved by induction on the packed recurrence `z_{t+1}=z_t⊕((z_t≪1)∨(z_t≪2))`. Kill if the bits `c_{2^n}` and `c_{2^n+2^{n-1}}` mix in a way that no 2-automatic relation holds on `n≤16` (already the 2-kernel is full through `k=12` on length-128 prefixes). Not a census of finite rows.

5. **q=8 mixing bit as a driven 1-D CA with vacuum right — Problem 1**

   Isolated-zero `q=8` still has a residual strip SCC. Drive the right half by the centre word `01^8` from a finite right and reconstruct `σ`. Kill if `σ` takes both values infinitely often on every width-`≤6` right (mixing, Jen does not fire). Survive if every finite right makes `σ` eventually periodic (then Jen excludes `q=8` for every finite seed). Distinct from the 20-state full 2-shift on freely supplied `σ`.
