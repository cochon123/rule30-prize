**Do not rerun unique-even \(G\) (only \(2^b-2\) and \(2^b-4\)),
exhaustiveness of unique \(D\) as the Cycle AS \(2\)-family and
\(3\)-family, the period-4 tails of \(e_{13}\) through \(e_{17}\), or
the vanishing of packed bits 13 and 17.** Cycle AV: unique-Green bits
for \(I_k\) are exactly those \(2k-2\) family members. Unique
\(3\)-family \(p\ge 13\) is not identically 0 (fires at \(k=15\),
\(j=k-3\)). The double \(p=3T/2+1\) does not have XOR 0 (fails at
\(k=12\)). Nested left is not a closed form. These are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle AV the
   unique scaling remainder is the \(2\)-family \(j\ge 5\)
   (\(p=33,65,\ldots,T+1\), including centre-right \(c_T\land r_T\))
   and the \(3\)-family \(j\ge 3\) (\(p=25,\ldots,3T/8+1\), which
   fires at \(k=15\)). Higher-multiplicity bulk in \(B^{\ge 14}\) is
   still uncontrolled. Do not hunt another finite nested-left family.
   Do not treat \(I^{\mathrm{left}}=0\) as proved. Exactly two
   doubles remains a prefix.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
