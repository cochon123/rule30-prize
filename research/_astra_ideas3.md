**Stop.** I read all five new notes. I cannot identify a remaining attack meeting your exclusions with a concrete proof mechanism. I recommend running none.

My round-2 rigidity recommendation was mistaken: given Jen/Kopra, \(A_p\Rightarrow B_p\) is equivalent to \(\neg A_p\), so it merely restates the target. Also, the \(q=8\) computations establish **no invariant found**, not nonexistence of a 2–3-site invariant; continuing those closures would nevertheless be another finite-table extension without a new argument.

The precise remaining mathematical gap is a consequence of the single-seed dynamics, valid uniformly over arbitrary onset times and spatial depths, that excludes the surviving periodic-center continuations while retaining their compatibility with the expanding support and exact ancestry. Existing abstractions lose that compatibility; enforcing it exactly reconstructs the triangle. For Problem 1, what remains to establish is \(\forall p>0\,\forall T\,\exists t\ge T:\ c_{t+p}\ne c_t\); neither adjacent-column rigidity nor persistent boundary disagreement supplies an independent intermediate result. Problem 2 still lacks a deterministic cancellation or discrepancy estimate on this particular orbit. Problem 3 still lacks either an exact algorithm meeting the linear-time threshold in an appropriate cost model or a lower bound covering arbitrary algorithms. The failed constructions supply neither.

This is a stopping judgment about the reviewed program, not an impossibility theorem. I have no new global lemma with independently justified, strictly weaker hypotheses to propose.