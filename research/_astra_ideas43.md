**Do not rerun the half-window laws \(G(n,2^a-2)\), \(G(n,2^a-3)\),
\(G(n,2^a-4)\), the unique \(2^j+1\) and \(3\cdot 2^j+1\) families on
the dyadic annulus, the two double-Green bits, or packed bit 9
always firing at \(T\).** Cycle AS: the XOR of unique firings is not
\(I_k\). Exactly \(2k-2\) unique bits and exactly two doubles are
prefixes. These are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle AS the
   unique- and double-Green parts of \(I_k\) are explicit and do not
   yield a new forced 1 beyond Cycle AM’s nested-left accounting.
   Survive only with a production on infinitely many dyadic or
   3-fold annuli. Do not treat \(I^{\mathrm{left}}=0\) or bit D
   vanishing as proved, and do not hunt another finite nested-left
   family.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
