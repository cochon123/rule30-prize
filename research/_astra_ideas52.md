**Do not rerun no-even-sextuples, the five-seed odd-lift orbit,
ten even septuple seeds, the ten septuple families, or bit 22
contributing 1.** Cycle BB: sextuple XOR and septuple XOR are not
\(I_k\). Nested left is still not a formula for \(I_k\). These are
proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle BB,
   multiplicity 1–7 Green bits are classified. The live remainder in
   \(B^{\ge 20}\) is multiplicity \(\ge 8\) together with the scaling
   unique \(2\)-family \(j\ge 5\) (bit 33 firing at \(T\) is still
   PREFIX) and unique \(3\)-family \(j\ge 4\) (the \(j=3\) member
   \(p=25\) never fires; the family still fires at \(k=15\) on
   \(j=k-3\)). Do not hunt another finite nested-left family. Do not
   treat \(I^{\mathrm{left}}=0\) or bit 33 always firing as proved.
   Do not assume every \(e_j\) is eventually period 4 (\(e_{29}\) has
   period 8).

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
