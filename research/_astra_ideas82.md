**Do not rerun unique through pentuple Green exhaustiveness on the
3-fold, 5-fold, or 9-fold Fermat annuli, Fermat \(p=1\) times,
Mersenne centre-right unique bits, the covering
\(\varphi^{(3)},\varphi^{(5)},\varphi^{(9)}\) through \(k=15\), the
\(\chi(T)\) closed form, the net bit-1 parity \(P(q,T)\)
recurrences, the even-spine covering-failure criterion through
\(k=12\), \(\gamma_k\) through \(k=15\), the bit-1 cone on \(B,C\),
low-bit silence / unique \(p=2U+2\) on \(B,C\), the four
leftmost unique bits \(p=2U+2,\ldots,2U+5\) on \(B,C\), the
freshman-defect identities for \(M=2^a\), the small-degree sums
\(S(2^a,d)\), defect chaining on \(B\), the left-machine
autonomy / packed-bit-2 and -4 identities, the Green-shift /
\(k=1,2\) period-\(H\) / unique-attractor \(k=3,4\) identities, the
reset-lift wrap / toggle identities, the ident-0 twin /
2-copy XOR identities, the \(a=b\Rightarrow u\equiv 0\) /
no-consecutive-ident-0 identities, the \(U=B\) iff \(A=DB\) /
scar-not-derivative identities, the half-xor intertwiner /
\(D\)-kills-odd-\(2\)-copy identities, the spatial-AND /
later-ident-0-iff-AND-triple identities, the AND-stay /
\(d=Da\) / no-enter-\((e,d)=(0,1)\) identities, the
\(d=Da\) iff \(c\to a\) / \(c\to b\) forbids \(11\) identities,
or the ident-1 iff ident-0-shift-2 /
consecutive-ident-1-only-\(01\) identities.**
Cycles BG–CF: those unique through pentuple bits are not
identically-1 productions, \(d_C\) chains through \(B\), and
\(J_B^{\to 2U}=0\) reduces to the left-word period-\(H\) seed
\(F^H(L_k(2W))=L_k(2W)\). That seed holds if every lift has at most
one odd-weight toggle, equivalently no later AND-triple after the
first odd ident-0. Nested left is still not a formula for
\(I_k\). There is no linear invariant of \(B\oplus U\).
\(2\)-copy type is not invariant, the half-xor obstruction is not
absorbing on arbitrary drives, \(011\) is not a universal witness,
\(S\) is not absorbing, \(c\to a\) is not absent after the scar,
\(c\) can lack \(11\)s, and both implications with \(c\not\equiv 0\)
occur off the prize orbit. These are proposals.

1. **Prove both implications hold together only when \(c\equiv 0\),
   on the prize orbit after the scar**

   This is false on the whole affine space (length-\(4\) windows).
   After ident-\(1\), which is tied to ident-\(0\), no packed bit
   implies the next two on the \(k=8\) lift (prefix). Prove that
   on the prize lift after the first odd ident-\(0\). Then the only
   AND candidate is the scar triple with \(c\equiv 0\), which fails
   because \(T\not\equiv 0\). Then \(r\le 1\), the seed holds, and
   \(J_B^{\to 2U}=0\). Then prove covering failure never occurs.
   Do not drop \(d_B\) or \(d_C\). Do not classify further unique
   bits on \(B,C\) unless a covering production appears. Do not hunt
   another finite nested-left family. Do not push the Fermat
   covering table to \(k=16\). Do not claim period \(H\) from time
   \(W\). Do not claim even \(\pi\)-weight of all bits. Do not
   claim at most one ident-\(0\) per lift. Do not hunt another
   linear syndrome of \(B\oplus U\). Do not treat \(2\)-copy type
   as invariant. Do not treat the half-xor obstruction as absorbing
   on arbitrary drives. Do not treat always-\(011\) as a witness.
   Do not treat \(S\) as absorbing. Do not claim \(c\to a\) never
   after the scar. Do not claim \(c\) always has a \(11\). Do not
   treat both implications \(\Rightarrow c\equiv 0\) as orbit-free.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
   Septuple families still grow with \(k\); that is the treadmill.
