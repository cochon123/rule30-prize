**Do not rerun bit 16 as Jacobsthal \(S_4\), the period-4 tail of
\(e_{18}\), the vanishing of bits 16 and 18, or
\(I_k=1\oplus B^{\ge 19}\).** Cycle AY: nested depth 18 is not a
closed form. Unique/double/triple Green bits remain as in AX. These
are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle AY the
   nested-left remainder starts at packed bit 19 (a quad seed). The
   live remainder is multiplicity \(\ge 4\) in \(B^{\ge 19}\) together
   with the scaling unique \(2\)-family \(j\ge 5\) and unique
   \(3\)-family \(j\ge 3\) (fires at \(k=15\)). Do not hunt another
   finite nested-left family. Do not treat \(I^{\mathrm{left}}=0\),
   bit 33 always firing, or exactly five quads as proved. Do not
   assume every \(e_j\) is eventually period 4 (\(e_{29}\) has period
   8).

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
