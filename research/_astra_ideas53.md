**Do not rerun no-even-octuples, the eleven-seed odd-lift orbit,
ten even nonuple seeds, or the ten nonuple families.** Cycle BC:
octuple XOR and nonuple XOR are not \(I_k\). Nested left is still
not a formula for \(I_k\). Unique 3-family XOR 0 already died at
\(k=15\) (Cycle AV); it is 1 at \(k=15,16\) only as a prefix. Bit 33
firing through \(k=16\) is still a prefix. These are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle BC,
   multiplicity 1–9 Green bits are classified. The live remainder in
   \(B^{\ge 20}\) is multiplicity \(\ge 10\) together with the scaling
   unique \(2\)-family \(j\ge 5\) and unique \(3\)-family \(j\ge 4\).
   Do not hunt another finite nested-left family. Do not treat
   \(I^{\mathrm{left}}=0\) or bit 33 always firing as proved. Do not
   assume every \(e_j\) is eventually period 4. Even \(|S|=10\) does
   not die by \(a=12\); even \(|S|=11\) is still growing.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
