**Do not rerun the two-point congruence
\(G(m,3\cdot 2^k-1)\), the uniqueness of the packed-bit-1 hit for
\(\theta_k\), the independence of \(N(q)\) from \(k\), or the unique
\(q=5\) hit at \(t=2^{k+1}\).** Cycle AO: the two-point family
\(p=3(2^k-2^j)+1\) is bulk but not a formula for \(\theta_k\). These
are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle AO,
   \(\theta_k=1\oplus S_k\) with \(S_k\) free of packed-bit-1 hits,
   but \(S\equiv 0\) is still killed. Survive only with a production
   on infinitely many dyadic or 3-fold annuli. Do not treat
   \(I^{\mathrm{left}}=0\) as proved, and do not hunt another finite
   nested-left family.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
