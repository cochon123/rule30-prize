**Do not rerun unique through pentuple Green exhaustiveness on the
3-fold, 5-fold, or 9-fold Fermat annuli, Fermat \(p=1\) times,
Mersenne centre-right unique bits, the covering
\(\varphi^{(3)},\varphi^{(5)},\varphi^{(9)}\) through \(k=15\), the
\(\chi(T)=G(2T-1,3T-1)\) closed form, the net bit-1 parity
\(P(q,T)\) recurrences, the even-spine covering-failure criterion
through \(k=12\), \(\gamma_k\) through \(k=15\), or the bit-1 cone
on covering blocks \(B,C\).**
Cycles BG–BQ: those unique through pentuple bits are not
identically-1 productions, \(\Theta(3\cdot 2^k)\) is not identically
1, net packed-bit-1 parity 1 is not a covering production,
\(\varphi^{(6)}_k\) is not identically 1 for \(k\ge 5\), the
dangerous even-spine alignment is empty only as a prefix,
\(\gamma_k\) is not identically 0, and blocks \(B,C\) have zero
bit-1 hits for every \(k\). Nested left is still not a formula for
\(I_k\).
These are proposals.

1. **Prove \(S_A=1\) and \(S_B=S_C=0\) never occur together**

   Covering fails at \(k+1\) iff the non-bit-1 remainder on
   \([2U,6U)\to 6U\) is 1 and the full remainders on
   \([6U,10U)\to 10U\) and \([10U,18U)\to 18U\) are 0 (those two
   have no bit-1 hits). A theorem that this never happens is the
   Fermat covering for all \(k\ge 3\), which kills every eventual
   period \(2^m\). Do not treat \(\Theta(6\cdot 2^k)\equiv 1\) as
   proved. Do not treat \(\gamma_k\equiv 0\) as proved. Do not hunt
   another finite nested-left family. Do not treat \(P=1\) as a
   1-production. Do not push the Fermat covering table to \(k=16\).

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
   Septuple families still grow with \(k\); that is the treadmill.
