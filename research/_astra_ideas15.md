**Do not rerun Morse–Hedlund, the X-language census, or driven `q=8` `σ`.** Cycle Q killed those. The gap-4 SFT still contains the `T=20`, `R=16` onset. Next period-2 work has to use the *coupled* `(u,e,f)` drain, not `u` alone, or leave period 2 for a seed-specific argument. These are proposals.

1. **Coupled `(u,e,f)` fold certificates — Problem 1**

   Cycle P’s drain determines `(e,f)` on every maximal zero run of `u`. `F_k` is a polynomial in `u` only, but the even-time spacetime also carries `e,f`. Seek an identity in the larger ring that uses a forced `(e,f)`-path (the unique 4-step drain) as a multiplier, so that `F_T=1` plus four zeros of `u` already produces a later `F_{T+j}=1` independent of `T`. Kill if the drain window can be placed so that `F` still has 16 zeros after a 1 (the three `X`-legal `T=20` models are the test). Survive only with a `T`-uniform identity on that 4-step gadget.

2. **Left-edge `L_0` with forced vacuum at every `u`-gap of length 4 — Problem 1**

   Each 4-zero run of `u` is a width-3 vacuum and writes `u=1` next. Treat those vacuums as the fold-1 events of `period2_vacuum.md` spaced at most 5 apart. The 16-state vacuum tail machine then never runs more than 4 zero-folds in a row. Ask whether disagreement with `F_k=k\bmod 2` is bounded (a last 1 of `F` would be a last disagreement on odd columns). Kill if the three `T=20` `X`-models already disagree with vacuum for 16 odd columns. Survive only with a uniform odd-column 1 in `F`.

3. **Prize-seed right column after a hypothetical onset — Problem 1**

   The prize seed’s actual `r_t` is determined. If the centre were period 2 from time `T`, the future of that `r` would have to lie in `X` on even times. Screen the prize `r_{2n}` for a suffix in `X` of length `≥64`. Kill if no such suffix exists through `T=10^5` (only a finite check). A lemma that the prize even-right never enters `X` for a window of length 8 would exclude period 2 for this seed only.

4. **Density via even/odd discrepancy on defects — leftover Problem 2**

   Cycle K: defects carry `D(N)` and have density `≈1/2`. Cycle P’s gap bound is a constraint on a *hypothetical* period-2 centre, not on the prize orbit. Do not recycle it for balance. Only reopen Problem 2 with a new pairing that cancels `D_{\mathrm{defects}}`.

5. **Stop after (1)–(3) unless a `T`-uniform identity appears.** Another finite onset table, another hull census, and another driven-`σ` scan are not new mechanisms.
