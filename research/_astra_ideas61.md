**Do not rerun unique through pentuple Green exhaustiveness on the
5-fold and 9-fold Fermat annuli, Fermat \(p=1\) times, Mersenne
centre-right unique bits, or the covering
\(\varphi^{(3)},\varphi^{(5)},\varphi^{(9)}\) through \(k=15\).**
Cycles BG–BK: those unique through pentuple bits are not
identically-1 productions. Nested left is still not a formula for
\(I_k\). These are proposals.

1. **Prove the Fermat covering for all \(k\ge 2\), or some
   \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\) infinitely
    often**

   A theorem that for every \(k\ge 2\) at least one of
   \(\varphi^{(3)},\varphi^{(5)},\varphi^{(9)}\) is 1 would kill every
   eventual period \(2^m\). Unique, triple, and pentuple Green
   remainder (the odd part through multiplicity 5) on the covering
   annuli is now closed; the obstruction is septuples and higher odd
   multiplicity, together with even \(S_{\mathrm{rest}}\) which does
   not affect the spines. Do not hunt another finite nested-left
   family. Do not treat \(I^{\mathrm{left}}=0\) as proved. Do not
   treat \(c_{5\cdot 2^k}\land r_{5\cdot 2^k}=0\) as proved. Do not
   treat the 9-fold pentuple XOR as \(\varphi^{(9)}\) (it fails at
   \(k=10\)).

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
   Septuple families on the 5-fold annulus still grow with \(k\);
   that is the treadmill, not a covering closed form.
