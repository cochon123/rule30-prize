**Do not rerun the 3-fold remainder \(\theta_k=c_{3\cdot 2^k}\oplus
c_{2^k}\), the induction \(G(2^{k+1}-1,3\cdot 2^k-1)=1\), or
formulas for \(\theta_k\) as \(I_k\), \(\bigoplus d\), or
\(\bigoplus(c\land r)\).** Cycle AJ: period \(2^m\) forces
\(\theta_k=0\); the leftmost 11 always hits \(\theta_k\), so
\(\theta_k=1\oplus S_k\); \(S\) is not identically 0. These are
proposals.

1. **\(\theta_k=1\) infinitely often, or \(I_k=1\) infinitely often**

   Either production kills every eventual period \(2^m\). Period
   \(2^m\) requires both \(I_k\to 0\) and \(S_k\to 1\). Survive only
   with a production on infinitely many 3-fold or dyadic annuli, not
   a local formula already killed in Cycles Z–AJ.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Nested edges, local Green remainders, and lag
   \(2^k\) from a `00` are not a second handle.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
