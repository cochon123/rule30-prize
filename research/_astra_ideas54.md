**Do not rerun period-8 tails \(e_{29}\)–\(e_{33}\), bit 33 contributing
1 for \(k\ge 7\), or bit 29 never firing.** Cycle BD: nested left is
still not a formula for \(I_k\). Unique \(2\)-family \(j\ge 6\)
(\(p=65,129,\ldots,T+1\)) is still open. These are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle BD, bit 33
   is a theorem, not a prefix. The live remainder in \(B^{\ge 20}\) is
   multiplicity \(\ge 10\) together with unique \(2\)-family \(j\ge 6\)
   and unique \(3\)-family \(j\ge 4\) (XOR 0 died at \(k=15\); \(k=15,16\)
   is a prefix). Do not hunt another finite nested-left family. Do not
   treat \(I^{\mathrm{left}}=0\) as proved. Do not assume every \(e_j\)
   is eventually period 4 or 8 (\(e_{48}\) has no small period on a
   512-window). Even \(|S|=10\) does not die by \(a=12\); even
   \(|S|=11\) is still growing.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
