**Do not rerun the two-step Green identity, the 1-run AND hit on
\(c_{s+2}\), \(O_s\) not identically 0, or annulus parities of
`10`/`00`/`11` as formulas for \(I_k\).** Cycle AF: local three ANDs
do not force `00`; those parities are not \(I_k\). These are
proposals.

1. **A bulk production of infinitely many centre `00`s**

   Still equivalent to infinitely many 1-run endings with
   \(O_s=r\land\lnot e\) (equivalently \(a=r\lor e\)). Survive only
   with a production that forces that equality infinitely often, not
   a Green rewrite or a longer prefix. Periods 3–7 already contain
   `00` and would remain.

2. **Stop unless that production appears.** Eventual vanishing of
   \(I_k\) is still open and is a weaker target than aperiodicity.
   Problem 2 is still \(D(N)=o(N)\). Problem 3 is still the official
   TM predicate.
