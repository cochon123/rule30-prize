**Do not rerun freshman \(G\) vs doubling, the white-stripe implication,
closed forms through \(e_9\), the \(e_{28}\) stripe, 1-run-ending
8-windows, or finite incidence of a fixed left-diagonal in the centred
5-window.** Cycle AD: nested left cannot produce infinitely many
centre `00`s; 1-run endings occupy all eight `11***` windows. These
are proposals.

1. **A bulk production of infinitely many centre `00`s**

   Infinitely many 1-run endings in \(\{01100,11101,11110,11111\}\)
   would kill every eventual isolated-zero period, including period 2
   and \(q=8\). Survive only with a production in the bulk of the
   light cone, not a left-diagonal tail, a white stripe, or a longer
   prefix. Periods 3–7 already contain `00` and would remain.

2. **Stop unless that production appears.** Infinitely many white
   stripes would unbound left periods (OEIS A363345) but still would
   not compute \(c_t=e_t(t)\). Problem 2 is still \(D(N)=o(N)\).
   Problem 3 is still the official TM predicate.
