**Do not rerun unique-even \(G\), unique-\(D\) exhaustiveness, bits 13
and 17, even-double obstructions, or exactly-two double \(D\)
(\(2^{a-2}-1\) and \(3\cdot 2^{a-3}-1\)).** Cycle AW: unique-Green
bits are the AS families (AV) and double-Green bits are exactly
\(p=3T/2+1\) and \(p=5T/8+1\). All \(e_j\) period 4 is false. Bit 33
firing at \(T\) is a prefix. Nested left is not a closed form. These
are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle AW the
   unique and double Green bits are classified. The live remainder
   is higher-multiplicity bulk in \(B^{\ge 14}\) together with the
   scaling unique \(2\)-family \(j\ge 5\) (\(p=33,65,\ldots,T+1\))
   and unique \(3\)-family \(j\ge 3\) (\(p=25,\ldots,3T/8+1\), fires
   at \(k=15\)). Do not hunt another finite nested-left family. Do
   not treat \(I^{\mathrm{left}}=0\) or bit 33 always firing as
   proved. Do not assume every \(e_j\) is eventually period 4.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
