**Do not rerun the all-\(q\) identity
\(c_{q 2^k}=c_{2^k}\oplus J\), the cancellation
\(G(q-1,q)=G(q-1,q-2)=v_2(q)\bmod 2\), or the leftmost-11 parity
\(P(q)=1\oplus\mathrm{popcount}(q)\).** Cycle AL: period \(2^m\)
forces \(\varphi^{(q)}=0\) for every integer \(q\); odd-\(q\) chained
AI extras stay outside; \(P\equiv 1\) for every odd \(q\) is killed
(\(q=7\)). These are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). Even-popcount \(q\)
   still have \(\varphi=1\oplus S\); no GF(2) linear combination of
   unshifted \(\varphi^{(q)}\) for
   \(q\in\{2,3,4,5,6,7,9,10,11,12,13,15,17\}\) is the constant 1 on
   \(k\le 15\) (rank 13 on length 16). A 7-term sum
   \(\varphi^{(2,6,7,9,10,11,17)}\) vanishes on \(k=2..15\) and dies
   at \(k=16,17\) — a prefix accident, not a relation. Extra Green
   hits for \(q=3\) are bulk (already 27 others at \(k=3\)), not a
   local formula for \(S\). Survive only with a production on
   infinitely many of these intervals.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
   Thue–Morse has Fermat \(\varphi\equiv 1\) (not 0); vanishing of
   every even-weight \(\varphi\) would exclude TM but is unproved for
   Rule 30.
