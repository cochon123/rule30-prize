**Do not rerun local formulas for \(I_k\) (3-window at half-time,
unweighted 11-parity, right-edge AND).** Cycle Z: \(b_k=b_{k-1}\oplus I_k\)
with \(I_k\) the Green parity of AND injections on \([2^{k-1},2^k)\).
\((b_k)\) is eventually constant iff \(I_k\) is eventually 0. These are
proposals.

1. **Infinitely many \(I_k=1\) — Problem 1**

   One uncancelled Green-hitting 11 in each dyadic annulus would flip
   \(b_k\) infinitely often, so \((b_k)\) is not eventually constant,
   hence \(v_k\ne v_{k+1}\) for all \(k\). Survive only with a CA
   production (for example a family of times \(2^k-1-2^a\) whose
   three Freshman bits have odd AND-parity and are not cancelled by
   the complementary \(m\)). Non-eventual-periodicity of \((b_k)\) is
   still required for an infinite 2-kernel.

2. **Stop unless that production appears.** Sibling-split indices,
   Boolean templates for \(b_k\), finite-window currents, and onset
   tables are not new mechanisms. Problem 2 is still \(D(N)=o(N)\).
   Problem 3 is still the official TM predicate.
