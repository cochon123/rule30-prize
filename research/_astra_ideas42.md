**Do not rerun the Green lift \(G(m,q\cdot 2^j-1)\), the Fermat-odd
reduction that recovers AO/AP, the closed times of the five
triple-Green bits, or the four quadruple-Green bits on the 3-fold
annulus.** Cycle AR: those nine bits are not identically-1
productions; bit B’s firing XOR is not \(k\bmod 2\) (dies at \(k=9\)).
Exactly-five triples, exactly-four quadruples, and bit D never firing
are prefixes. These are proposals.

1. **Some \(\varphi^{(q)}_k=1\) infinitely often, or \(I_k=1\)
   infinitely often**

   Either kills every eventual period \(2^m\). After Cycle AR the
   multiplicity-3 and multiplicity-4 parts of \(\theta_k\) are
   explicit and do not yield a new forced 1. The obstruction is still
   the bulk of higher-multiplicity bits. Survive only with a
   production on infinitely many dyadic or 3-fold annuli. Do not
   treat \(I^{\mathrm{left}}=0\) or bit D vanishing as proved, and do
   not hunt another finite nested-left family.

2. **A bulk production of infinitely many centre `00`s**

   Still the cut that would kill every isolated-zero eventual period,
   including \(q=8\). Residual periods 3–7 already contain `00`.

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
