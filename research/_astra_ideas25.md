**Do not rerun \(I_k=\) XOR of \((c\land r)\) on the annulus, Mersenne
3-windows, small recurrences for \(I_k\), or local formulas already
killed in Cycles Z–AA.** Cycle AA: \(I_k=\bigoplus_t(c_t\land r_t)\oplus R_k\)
with \(R_k\) the off-centre AND-Green remainder. These are proposals.

1. **Control \(R_k\) — Problem 1**

   A proof that \(R_k\) equals the \((c\land r)\) parity only finitely
   often (or a CA identity for \(R_k\) that is not eventually equal to
   that parity) would make \(I_k\) not eventually 0. Survive only with
   such an identity. Non-eventual-periodicity of \((b_k)\) is still
   required for an infinite 2-kernel.

2. **Stop unless that identity appears.** Near-central \(v_2\) masks,
   leftmost 11, and Freshman \(m=2^a\) slices are documented and do not
   close \(I_k\). Problem 2 is still \(D(N)=o(N)\). Problem 3 is still
   the official TM predicate.