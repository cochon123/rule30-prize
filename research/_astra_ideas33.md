**Do not rerun the arbitrary-time dyadic step, causality of
\(J_{t,k}\), or palindrome-constraint window graphs at distances
\(1,2,4\).** Cycle AI: \(c_{t+2^k}=c_t\oplus x(t,\pm 2^k)\oplus J_{t,k}\)
with \(J\) a Boolean of the causal \(2^{k+1}+1\)-window; the
distance-1 graph recovers Cycle AB, and distances 2 and 4 have
mixing SCCs. These are proposals.

1. **Non-vanishing of \(I_k\)**

   Still the cut that would kill every eventual period \(2^m\).
   \(J_{2^{k-1},k-1}=I_k\) is the full half-time row, not a smaller
   window. Survive only with a production of odd Green parity on
   infinitely many dyadic annuli.

2. **A bulk production of infinitely many centre `00`s**

   A `00` at \(t\) does not force a `00` at \(t+2^k\). Nested edges
   and local Green remainders are not a second handle. This would
   kill every isolated-zero eventual period, including \(q=8\).

3. **Stop unless one of those productions appears.** Problem 2 is
   still \(D(N)=o(N)\). Problem 3 is still the official TM predicate.
